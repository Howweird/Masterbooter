# ============================================
# DISM Tool — GUI for DISM
# ============================================
# Graphical interface for DISM which works on:
# - Running OS (live Windows)
# - Mounted WIM images
# - Offline OS installations
# From pcassistsoftware.co.uk — .NET Framework 4.8 (needs WinPE-NetFx in PE)

[tool]
name = "DISM Tool"
description = "GUI interface for DISM - works on running OS, mounted WIM, or offline OS"
category = "system"
version = "1.0"
exe = "DISM_Tool.exe"
is_shell = false
create_shortcut = true
enabled_by_default = true
auto_launch = false

# Direct download from pcassistsoftware.co.uk
download_url = "https://www.pcassistsoftware.co.uk/downloads/DISM_Tool.7z"

# GitHub mirror — tried automatically if the primary download fails
fallback_url = "https://github.com/Howweird/MasterBooter-Tools/releases/download/v1.0/DISMTool.zip"
