# ============================================
# File Explorer (PE) — Dual-Pane File Manager
# ============================================
# Lightweight dual-pane file explorer with search facility.
# Works in WinPE and live Windows.
# Uses ShellBrowser components for native shell integration.
# From pcassistsoftware.co.uk — .NET Framework 4.8 (needs WinPE-NetFx in PE)

[tool]
name = "File Explorer"
description = "Lightweight dual-pane file explorer with search for WinPE and live Windows"
category = "shell"
version = "1.0"
exe = "File Explorer (PE).exe"
is_shell = false
create_shortcut = true
enabled_by_default = true
auto_launch = false

# Direct download from pcassistsoftware.co.uk
download_url = "https://www.pcassistsoftware.co.uk/downloads/File_Explorer_PE.7z"

# GitHub mirror — tried automatically if the primary download fails
fallback_url = "https://github.com/Howweird/MasterBooter-Tools/releases/download/v1.0/FileExplorer.zip"
